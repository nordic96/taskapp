import { SxProps } from "@mui/material";

export const pageContainerStyles: SxProps = {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    flexDirection: 'column',
    gap: 2,
};

export const labelStyles: SxProps = {
    padding: '4px 8px 4px 8px',
    borderRadius: 2,
};
